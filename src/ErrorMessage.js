import React from 'react';

const ErrorMessage = () => {
  return (
    <div>
      {/* Error Message */}
      <div>"Error: File upload failed. Please try again."</div>
      {/* Close Modal button */}
      <button>[Close Modal]</button>
    </div>
  );
};

export default ErrorMessage;
