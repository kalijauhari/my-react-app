import React from 'react';

const UploadProgress = () => {
  return (
    <div>
      {/* Progress Bar, Loading Spinner */}
      <div>Progress Bar: [========== 50%]</div>
      <div>Loading Spinner: [Processing...]</div>
    </div>
  );
};

export default UploadProgress;
